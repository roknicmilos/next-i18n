export interface LanguageMenuDictionary {
    activeLanguage: {
        label: string;
    },
    changeLanguage: {
        label: string;
    },
    allLanguages: {
        enLabel: string;
        deLabel: string;
        nlLabel: string;
    }
}
